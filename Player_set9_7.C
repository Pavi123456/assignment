#include <stdio.h>
#include<conio.h>
int main()
{
   long int a,k,i;
   clrscr();
   printf("Enter the number:");
   scanf("%d",&a);
printf("\nEnter the K value:");
   scanf("%d",&k);
printf("%d",a<<k);
getch();
return 0;
}